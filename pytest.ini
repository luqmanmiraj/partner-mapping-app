[pytest]
testpaths = tests
pythonpath = .
addopts = -ra --tb=short
filterwarnings =
    ignore::DeprecationWarning
markers =
    unit: fast tests for a single module or component
    integration: multi-module workflows and page render smoke tests
    isolation: boundaries and cross-partner / cross-session isolation
